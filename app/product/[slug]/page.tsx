import Link from 'next/link'; import { notFound } from 'next/navigation'; import { products } from '../../../data/products';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main className="inner-page product-detail"><Link href="/products" className="back">← Tous les produits</Link><div className="detail-grid"><div className="detail-image"><img src={p.image} alt={p.name}/></div><div><div className="eyebrow">{p.offerLabel}</div><h1>{p.name}</h1><p>{p.description}</p><h2>Pourquoi ce produit ?</h2><ul><li>Format pensé pour l’usage quotidien.</li><li>Présentation claire et sans promesse exagérée.</li><li>L’offre, le prix et les conditions finales sont consultés chez le marchand.</li></ul><Link className="primary" href={`/go/${p.slug}`}>Voir l’offre <span>→</span></Link><p className="fine">Certains liens présents sur Auto+ sont des liens affiliés. Une commission peut être perçue après un achat, sans coût supplémentaire pour l’acheteur.</p></div></div></main>}
