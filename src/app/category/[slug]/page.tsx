import Link from 'next/link'; import {products} from '../../../data/products';
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug.replaceAll('-',' ');const list=products.filter(p=>p.category.toLowerCase().replaceAll(' ','-')===slug);return <main><header className="header"><div className="nav"><Link href="/" className="brand">AUTO<span>+</span></Link><Link href="/products" className="nav-cta">Catalogue <span>→</span></Link></div></header><section className="section"><Link className="back" href="/products">← Catalogue</Link><div className="section-head category-title"><div><span className="eyebrow">CATÉGORIE</span><h1>{title.charAt(0).toUpperCase()+title.slice(1)}</h1></div><p>{list.length} sélection{list.length>1?'s':''} dans cette catégorie.</p></div><div className="product-grid">{list.map(p=><article className="card" key={p.id}><Link href={`/product/${p.slug}`} className="image-wrap"><span className="product-visual" aria-hidden="true">{p.image}</span></Link><div className="card-body"><div className="tag">{p.offerLabel}</div><h2>{p.name}</h2><p>{p.description}</p><div className="card-foot"><strong>{p.price}</strong><Link className="offer-link" href={p.affiliatePath}>Voir l’offre <span>→</span></Link></div></div></article>)}</div></section></main>}
