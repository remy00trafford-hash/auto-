import Link from 'next/link'
import { notFound } from 'next/navigation'
import { products } from '../../../data/products'
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main className="detail-page"><header className="header"><div className="nav"><Link href="/" className="brand">AUTO<span>+</span></Link><Link href="/products" className="nav-cta">Tous les produits <span>→</span></Link></div></header><section className="detail"><Link href="/products" className="back">← Retour aux produits</Link><div className="detail-grid"><div className="detail-image"><img className="product-image" src={p.image} alt={p.name} /></div><div className="detail-copy"><div className="tag">{p.offerLabel}</div><h1>{p.name}</h1><p className="lead">{p.description}</p><div className="detail-price"><span>Offre actuelle</span><strong>Consulter sur AliExpress</strong></div><div className="why"><h2>Pourquoi ce produit ?</h2><p>Une référence choisie pour son utilité quotidienne et la simplicité de son usage. Les informations affichées ici sont indicatives et doivent être vérifiées sur l'offre actuelle.</p></div><Link className="primary detail-cta" href={p.affiliatePath}>Voir l’offre <span>→</span></Link><p className="disclosure">Certains liens présents sur Auto+ sont des liens affiliés. Un achat après votre clic peut générer une commission pour Auto+, sans coût supplémentaire pour vous.</p></div></div></section></main>}
